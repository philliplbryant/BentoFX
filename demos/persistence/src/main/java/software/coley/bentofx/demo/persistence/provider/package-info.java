/**
 * This demo's implementations of the persistence provider interfaces the
 * application is expected to supply.
 *
 * <p>Each is constructed by the application and handed to the persistence API as an
 * argument. None is discovered at runtime - the replaceable parts of the framework
 * are the codec and storage implementations, which live in their own modules.</p>
 *
 * @author Phil Bryant
 */
package software.coley.bentofx.demo.persistence.provider;
